<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.calensiee">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Calensiee">
        <activity android:name=".EventEditActivity"/>
        <activity android:name=".View.WeekViewActivity" />
        <activity android:name=".View.MenuView" />
        <activity android:name=".View.MonthView" />
        <activity android:name=".EmailPasswordActivity" />
        <activity android:name=".AccountCreate" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".View.MonCompteActivity" />
        <activity android:name=".View.ALaUneActivity" />
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>